{
  "name": "fft-typescript",
  "version": "1.0.0",
  "description": "FFT implementation in TypeScript using standard library only",
  "main": "fft.js",
  "scripts": {
    "build": "tsc",
    "start": "tsc && node fft.js",
    "test": "tsc && node test_fft.js"
  },
  "keywords": ["fft", "signal-processing"],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  }
}
